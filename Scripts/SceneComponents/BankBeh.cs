using UnityEngine;
using System;
using System.Collections;

public class BankBeh : MonoBehaviour {
/*
	public static event EventHandler GiveInterest_event;
	private void OnGiveInterestEvent (EventArgs e)
	{
		if(GiveInterest_event != null)
			GiveInterest_event(this, e);
	}
	private DateTime startingTime;
	private DateTime currentTime;

    private int accountBalance;

	void Awake() {
		DontDestroyOnLoad(this);
	}

	// Use this for initialization
	void Start () {
		startingTime = DateTime.UtcNow;
	}
	
	// Update is called once per frame
	void Update ()
	{
		currentTime = DateTime.UtcNow;
		TimeSpan elapsed = currentTime - startingTime;
		if (elapsed.TotalMinutes > 2) {
            print("Increase available money");
            this.CalculationInterest();
		}
	}

    private void CalculationInterest()
    {
        accountBalance = Mz_StorageManage.AccountBalance;
    }
*/
}
